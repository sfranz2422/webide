Flask==3.0.3
SQLAlchemy==2.0.35
gunicorn==23.0.0
psycopg2-binary==2.9.9
Authlib==1.3.2
# Authlib does not declare an HTTP client of its own, but its Flask
# integration imports authlib.integrations.requests_client on the way in.
requests==2.32.3
